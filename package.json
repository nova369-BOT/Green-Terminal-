{
  "name": "green-terminal",
  "version": "0.1.0",
  "private": true,
  "description": "GREEN TERMINAL \u2014 chart analysis, automation and execution unified in one trading terminal.",
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node scripts/test.js && node scripts/ui-smoke.js",
    "test:engine": "node scripts/test.js",
    "test:ui": "node scripts/ui-smoke.js",
    "calibrate": "node scripts/calibrate.js",
    "check": "for f in server.js src/modules/*.js src/ui/*.js src/app/*.js scripts/*.js; do node --check \"$f\" || exit 1; done; echo 'syntax ok'"
  },
  "devDependencies": {
    "jsdom": "^30.1.1"
  },
  "keywords": [
    "trading-terminal",
    "charting",
    "algorithmic-trading",
    "backtesting",
    "automation"
  ],
  "license": "UNLICENSED"
}
